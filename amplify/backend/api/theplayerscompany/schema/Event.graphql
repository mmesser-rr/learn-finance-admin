type Event @model @searchable {
  id: ID!
  creatorId: ID!
  creator: Athlete! @hasOne(fields: ["creatorId"])
  sponsor: String!
  title: String!
  tagline: String!
  description: String!
  date: String!
  time: String!
  location: String!
  reward: Int!
}